# ============================================================
# Asteria OS — Rust OSDev .gitignore (v0.1)
# Combines Cargo defaults + OS images + QEMU + editors.
# ============================================================

# ---- Rust / Cargo -----------------------------------------------------------
# Generated by Cargo — compiled files and executables
/target/
/debug/

# Keep the lockfile tracked for reproducible builds
!Cargo.lock

# Backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these (debug info)
*.pdb

# Mutation testing output (if you ever use cargo-mutants)
**/mutants.out*/

# ---- Build artifacts / staging ---------------------------------------------
/dist/
/build/
/out/

# ---- OS images & tool outputs ----------------------------------------------
*.img
*.iso
*.bin
*.efi
*.elf
*.map
*.o
*.a

# ---- QEMU / firmware blobs & logs ------------------------------------------
.qemu/
*.fd              # e.g., OVMF_CODE.fd, OVMF_VARS.fd
*.log

# ---- Editors / IDEs / OS cruft ---------------------------------------------
.vscode/
# JetBrains: prefer a global ignore; uncomment to ignore project files locally
#.idea/

*.swp
*.swo
.DS_Store
Thumbs.db

# ---- Environment / local overrides -----------------------------------------
.env

# ---- Governance manifests: keep these TRACKED -------------------------------
!manifests/
!manifests/*.json
