# ============================================================
# Asteria OS — Rust OSDev .gitignore (v0.2)
# Cargo defaults + OS images + QEMU + editors + dumps.
# ============================================================

# ---- Rust / Cargo -----------------------------------------------------------
/target/
/debug/
**/*.rs.bk               # rustfmt backups
*.pdb                    # MSVC debug info (Windows)
!Cargo.lock              # keep lockfile tracked for reproducibility

# ---- Build artifacts / staging ---------------------------------------------
/dist/
/build/
/out/

# ---- OS images & tool outputs ----------------------------------------------
*.img
*.iso
*.bin
*.efi
*.elf
*.map
*.o
*.a
*.dylib
*.so
*.dll

# Painter / debug frame dumps (devhost)
*.ppm

# Archives produced locally (CI uploads artifacts instead)
*.zip
*.tar
*.tgz
*.tar.gz

# ---- QEMU / firmware blobs & logs ------------------------------------------
.qemu/
*.fd                     # e.g., OVMF_CODE.fd, OVMF_VARS.fd (don’t commit firmware)
*.log

# ---- Crash dumps / perf -----------------------------------------------------
core
core.*
*.dmp
perf.data
*.prof
*.pprof

# ---- Editors / IDEs / OS cruft ---------------------------------------------
.vscode/
# JetBrains: prefer a global ignore; uncomment to ignore project files locally
#.idea/
*.iml
*.swp
*.swo
.DS_Store
Thumbs.db
.directory

# ---- Environment / local overrides -----------------------------------------
.env
.env.*
*.tmp
*.bak
*~

# ---- Governance & CI: keep these TRACKED -----------------------------------
!manifests/
!manifests/*.json
!.github/
!.devcontainer/
